# Tasks — Etapa 2: Salas

> Estado mixto: la Cocina está parcialmente implementada (178 líneas); Dormitorio, Baño,
> Sala de actividades y Huerta son placeholders de 33 líneas (rectángulo + emoji + "En construcción" + botón volver).
> `[x]` = verificado en el código. `[ ]` = pendiente (con nota de qué falta).

## 1. Infraestructura compartida de salas

- [x] 1.1 Definir el patrón/base de sala reutilizable (RoomScene o helpers: fondo escalado, `_toScene`, hover-objects, botón volver)
      <!-- Extraído a js/scenes/RoomScene.js (clase base, no aplicada aún a KitchenScene ni a las salas nuevas — eso es trabajo de las secciones 2-6) -->
- [x] 1.2 Implementar la clase reutilizable `TimingBar` (barra de timing con zona objetivo y callback de resultado)
      <!-- js/systems/TimingBar.js — cursor ida/vuelta, zona objetivo + zona "perfect" opcional, onResult('success'|'fail'|'perfect') -->
- [x] 1.3 Implementar el detector de gesto `SwipeGesture` (dirección + distancia mínima + ciclar opciones)
      <!-- js/systems/SwipeGesture.js — detecta dirección/distancia; ciclar opciones o contar repeticiones queda a cargo de quien lo usa (Baño, sección 4) -->
- [x] 1.4 Resolver la stat de higiene: NO se agrega stat nueva. Bañarse da un empujón chico a hambre + sueño + diversión (stats existentes vía StatsSystem); no requiere UI nueva
      <!-- Decisión del usuario (Open Question 1 resuelta): sin 5ª stat ni anillo nuevo. Bañarse llama a los add existentes de StatsSystem con un valor bajo. Implementación real queda en 4.3-4.5 (Baño), que es donde se dispara el efecto -->
- [x] 1.5 Definir inventario compartido + economía de monedas (mercadería de cocina, cosecha de huerta, compra de semillas)
      <!-- Modelo de datos implementado: js/data/products.js (24), js/data/recetas.js (5), js/systems/Inventory.js (get/add/remove/canCraft/craft, soporta ingrediente "anyOf"), registrado en BootScene vía registry.set('inventory', ...). Arranca vacío (coins=0): el origen del stock no-cosechable y el sistema de monedas siguen sin resolver (Open Question 2, parte 2) — no se inventó esa regla de negocio -->
- [x] 1.6 Bugfix: HelpScene/SettingsScene no bloqueaban los clicks de la sala de atrás (pedido del usuario, reportado durante testing)
      <!-- Bug: HelpScene y SettingsScene se lanzaban con this.scene.launch() SIN pausar la sala que las abría (a diferencia de MapScene, que sí hace this.scene.pause() antes de this.scene.launch('MapScene') — ver CabinScene _createNavButtons). El overlay oscuro de Help/Settings no es interactivo, así que un click en la zona oscura (ej. cerca de heladera/hornito/mercadería en la Cocina) pasaba de largo y llegaba a los objetos interactivos de la sala de abajo, abriendo su panel "por detrás" sin que el jugador lo viera hasta cerrar Help/Settings. Fix: HelpScene y SettingsScene ahora reciben callerKey en la data de launch (this.scene.key de quien las abre) vía init(data); quien las abre hace this.scene.pause() antes de this.scene.launch(); el botón "✕" de ambas hace this.scene.resume(this._callerKey) además de this.scene.stop(). Tocado: CabinScene (_createGearButton, _createHelpButton), KitchenScene (_createGearButton, _createHelpButton), RoomScene (_makeHelpButton, para que las salas futuras no hereden el mismo bug), HelpScene.js, SettingsScene.js. Verificado con Playwright: game.scene.isPaused('KitchenScene') es true con Ayuda abierta, un click en la posición real de la heladera mientras Ayuda está abierta ya NO deja _modalObjects seteado, y tanto Ayuda como Configuración resumen la sala correctamente al cerrarse (probado desde Cabaña y desde Cocina). -->
- [x] 1.7 Mapa de navegación con pergamino (pedido del usuario, reemplaza el botón "← Volver" de todas las salas)
      <!-- El usuario proveyó 3 assets: pergamino_sin_fondo.png (ícono de botón), mapa_sin_fondo.png (fondo ilustrado con las 5 salas ya dibujadas y rotuladas: Dormitorio/Cocina/Baño/Huerta/Sala de actividades, más un compás y "HOME"), items_sin_fondo.png (sheet con los mismos 5 íconos en transparente, para superponer sobre el mapa y capturar el click — mismo patrón que Cocina: fondo + objetos recortados encima).

      MapScene.js reescrita por completo: antes era un panel de texto con 5 filas + botón "Ir a la ciudad" deshabilitado; ahora dibuja mapa_fondo escalado por ANCHO de pantalla (la imagen es muy panorámica, 2816x1536 — ajustar por alto la desbordaría de lado a lado) y superpone los 5 íconos recortados (mapa_cama/mapa_horno/mapa_inodoro/mapa_planta/mapa_diana, extraídos de items_sin_fondo.png con un script Jimp de un solo uso, con padding de 6px) en la posición exacta de cada sala (MAP_BOXES, cajas relativas 0..1 medidas a mano con una grilla de referencia sobre el arte — la caja de Sala Actividades salió mal en el primer intento, ~300px corrida; se corrigió detectando el anillo azul del target por color con Jimp y usando su centro real como ancla, verificado visualmente después). Cada ícono escala "contain" dentro de su caja (no deforma, el arte de items_sin_fondo.png no tiene exactamente la misma proporción que la caja medida). "Ir a la ciudad" se mantuvo como texto deshabilitado debajo del mapa (el arte nuevo no lo incluye).

      Generalizado el patrón caller/resume (mismo de 1.6): MapScene.init(data) ahora lee callerKey en vez de asumir 'CabinScene' fijo — necesario porque ahora se abre desde CUALQUIER sala, no solo desde la Cabaña. _goToRoom hace scene.stop(this._callerKey) en vez de hardcodear 'CabinScene'; _close hace scene.resume(this._callerKey).

      El botón "← Volver" (texto) de KitchenScene, BedroomScene, BathroomScene, ActivityScene, GardenScene y RoomScene._makeBackButton (helper compartido, sin aplicar aún a las salas placeholder — ver nota en 1.1) pasa a ser el ícono pergamino; su pointerdown ahora hace this.scene.pause() + this.scene.launch('MapScene', { callerKey: this.scene.key }) + this.scene.bringToTop('MapScene') (mismo patrón que el fix de 1.6 — bringToTop hace falta porque MapScene está registrada en game.js ANTES que las salas, así que sin esto Phaser la dibuja debajo). cartel_casa de CabinScene actualizado para pasar callerKey también (antes lanzaba MapScene sin data).

      Verificado con Playwright: abrir el mapa desde Cabaña (cartel_casa) y desde Cocina (pergamino) con capturas de pantalla confirmando los 5 íconos alineados sin fantasmas/dobles; CabinScene/KitchenScene quedan pausadas mientras el mapa está abierto y se reanudan al cerrar con "✕"; tocar el ícono de Cocina en el mapa detiene la escena que lo abrió y arranca KitchenScene. -->

      <!-- PENDIENTE: BedroomScene/BathroomScene/ActivityScene/GardenScene siguen siendo placeholders (33 líneas, sin objetos reales) — el pergamino ya funciona en ellas pero el resto de la sala no cambió, eso es trabajo de las secciones 3-6 de este mismo tasks.md.

      Fix post-entrega (1): el usuario reportó que Cocina (horno) y Sala Actividades (diana) no quedaban encuadradas con el ícono dibujado en el mapa — la medición a mano con grilla fallaba justo en esos dos (la de Sala Actividades había salido ~300px corrida). Se recalcularon ambas con un método más preciso: anclar por color muy distintivo (llama naranja del horno, anillo azul del target) contra el pergamino tostado, ubicar ese mismo color dentro del ícono recortado en items_sin_fondo.png, calcular la escala items→mapa comparando ambas marcas, y proyectar el bounding box completo del ícono con esa escala — en vez de volver a medir a ojo. Dormitorio/Baño/Huerta no se tocaron (no reportados, y Dormitorio se validó con el mismo método por las dudas — coincidió con la medición original). Verificado con capturas ampliadas 3x de la zona del mapa.

      Fix post-entrega (2): el usuario pidió "el mapita más grande y más abajo, debajo del engranaje" — SE MALINTERPRETÓ como el contenido del mapa dentro de MapScene (se agrandó y se bajó ahí, ver _drawMap) cuando en realidad se refería al ÍCONO/BOTÓN del pergamino (el que reemplaza "Volver"), que seguía abajo de la pantalla sin cambios. Corregido: el botón pergamino de las 5 salas (Kitchen + 4 placeholders + RoomScene._makeBackButton) se movió de (W/2, H-34/H-40) a (W-46, 106) — misma columna que el engranaje, justo debajo. De paso salió un bug real: pergamino_sin_fondo.png tiene mucho margen transparente (el dibujo ocupa ~26% del alto del lienzo), así que escalando por TARGET_SIZE/tex.height (mismo patrón que engranaje/moneda_ayuda, que sí vienen recortados) el ícono renderizaba casi invisible. Se creó assets/sprites/pergamino_recortado.png (recorte al contenido real vía Jimp) y BootScene.preload() carga esa versión con la textura 'pergamino' (sin tocar los 6 call-sites). Además, como el pergamino es un cilindro ancho y bajo (no ~cuadrado), escalar por ALTO lo hacía desbordar el borde derecho de la pantalla — se cambió a escalar por ANCHO (TARGET_WIDTH=64) en los 6 lugares.

      Fix post-entrega (3): en el mismo hilo, el usuario reportó que en la Cocina "el mapa está debajo del panel de comidas rápidas" — verificado con Playwright pintando el overlay de MapScene de rojo opaco que el orden de escenas SÍ era correcto (MapScene renderiza arriba de todo). La causa real: el overlay semitransparente (0x000000 alpha 0.75, igual que Help/Settings) dejaba entrever el panel de comer rápido de Cocina, que también es oscuro (0x1e1b4b), dando la sensación de mezclarse/quedar debajo. Cambiado a opaco: 0x1a1a2e (el mismo backgroundColor del juego) a alpha 1. Verificado que ya no se filtra nada de la sala de atrás. -->

- [x] 1.8 Sistema de guardado general — localStorage (pedido del usuario, deadline 12/8, bloqueante si el juego se entrega por link en vez de APK)
      <!-- Qué: hasta ahora el juego no persistía NADA (ni stats ni inventario) — solo estaba planeada la persistencia de la Huerta (6.3). js/systems/SaveManager.js nuevo: save(stats, inventory)/load()/applyTo(stats, inventory, data), serializa a un objeto plano versionado (version:1) y lo guarda en localStorage bajo 'juego-vero-save-v1'. No conoce Phaser ni escenas.

      BootScene.create(): antes de registrar stats/inventory en game.registry, si SaveManager.load() devuelve algo, se vuelca con applyTo() sobre las instancias recién creadas (StatsSystem/Inventory arrancan con sus valores default de siempre y el save los pisa encima, no se cambia la forma del código según haya o no partida guardada).

      Autoguardado en game.js (no en una escena — stats/inventory viven en game.registry, que sobrevive a cambios de escena, así que no hace falta ninguna escena activa en particular): 3 disparadores — setInterval cada 10s (red de seguridad), window 'beforeunload' (cerrar pestaña/recargar en desktop), document 'visibilitychange' a 'hidden' (el que más importa en mobile: al cambiar de app o bloquear el celu, beforeunload no siempre dispara pero visibilitychange sí).

      Por qué: decisión de scope-cut para el deadline del 12/8 ([[project/deadline-aniversario]] en engram) — si el juego se entrega por link en vez de APK, el usuario pidió explícitamente que el progreso no se pierda al volver a entrar.

      Verificado con Playwright: localStorage vacío en partida nueva; tras modificar stats/inventario y guardar, localStorage tiene el JSON correcto; tras recargar la página (simula cerrar y reabrir el juego), stats e inventario quedan exactamente como estaban; disparar visibilitychange=hidden manualmente también guarda correctamente. -->

## 2. Cocina

- [x] 2.1 Fondo de la cocina escalado a la altura de la pantalla
- [x] 2.2 Ventana con cielo dinámico según la hora real (día/atardecer/noche) + refresco cada 60s
- [x] 2.3 Objetos interactivos con hover sutil (heladera, hornito, mercadería) vía `_makeHoverObject`
      <!-- La infraestructura de hover-zones se mantiene, pero el callback de cada zona CAMBIA (design D8): ya no da hambre directo; heladera→panel inventario, mercadería→panel alacena, hornito→Recetario. Ver 2.7-2.10 -->
- [x] 2.4 Ámbar durmiendo (`ambar_dormida`) sobre la heladera
- [x] 2.5 Mecánica "comer rápido" — migrar de tocar heladera/mercadería a panel fijo de flechas + botón Comer
      <!-- Implementado en KitchenScene._createComerPanel/_cycleComer/_eatQuick/_updateComerPanel. Las hover-zones de heladera/mercadería ya NO llaman a esto (ver 2.8/2.9); reutiliza el cooldown (this._feedCooldown) y el helper de texto flotante (ahora _showFloatingText, compartido con cocinar) de la mecánica vieja -->
- [x] 2.6 Botón volver a la cabaña
- [x] 2.7 Definir el catálogo de datos: `js/data/products.js` (24 productos) y `js/data/recetas.js` (5 recetas)
      <!-- Verificado contra design D9: 24 productos (4 alacena + 5 heladera + 10 cosecha + 5 plato) y 5 recetas con los ingredientes/hambre exactos del diseño. Ya estaba completo desde la Sección 1; solo faltaba precargar los sprites de cada producto, agregado ahora en BootScene.preload() (loop sobre PRODUCTS) porque las tareas 2.8-2.12 los necesitan para pintar íconos -->
- [x] 2.8 Panel modal de Heladera (lista inventario categoría `heladera`) reutilizando el patrón de modal de `MapScene`
      <!-- KitchenScene._openModal (overlay + panel redondeado + botón "✕", calcado de MapScene.create — sin tap-outside-to-close, MapScene tampoco lo tiene) + _openStoragePanel('heladera', 'Heladera') + _addInventoryRow (ícono/nombre/cantidad, gris si cantidad=0) -->
- [x] 2.9 Panel modal de Alacena (lista inventario categoría `alacena`) reutilizando el patrón de modal de `MapScene`
      <!-- _openStoragePanel('alacena', 'Alacena'), mismo patrón que 2.8 -->
- [x] 2.10 Panel modal de Recetario (5 recetas, ingredientes con check verde/gris, botón Cocinar habilitado/deshabilitado según inventario)
      <!-- _openRecetario + _addRecetaRow: ingredientes en verde/gris vía Inventory.has (soporta anyOf), botón "Cocinar" solo interactivo si Inventory.canCraft -->
- [x] 2.11 Cocinar con barra de timing única: al acertar consume 1 de cada ingrediente y sube hambre según receta; al fallar no consume nada
      <!-- _cocinar: cierra el Recetario y dispara UNA TimingBar; éxito → Inventory.craft(receta.id) + stats.addHunger(receta.hambre); fallo → no toca inventario ni stats, solo feedback visual -->
- [x] 2.12 Panel fijo de comer rápido con flechas ◀ ▶ + botón Comer, usando los productos `comestibleDirecto` del catálogo
      <!-- _createComerPanel: cicla Products.comestiblesDirecto() completo (no solo con stock); botón "Comer" deshabilitado si el producto actual tiene cantidad 0; si NINGÚN comestible directo tiene stock global, el panel avisa "Nada para comer" y deshabilita el botón (cubre el escenario "Sin productos comestibles disponibles" del spec) — esta regla de cuándo mostrar el aviso global vs. solo deshabilitar por ítem no estaba explícita en design D8, se señaló en el resumen de la tarea en vez de resolverla en silencio -->
- [ ] 2.13 Pedir/reemplazar el asset de fondo de cocina sin productos dibujados sobre la mesada (mesada despejada)
      <!-- Depende de un asset nuevo, no de código (design D8/Open Question 8); `cocina_nueva.png` actual tiene productos dibujados encima -->
- [x] 2.14 Ayuda contextual por sala (fuera del alcance original del proposal, pedido del usuario): HelpScene ahora recibe `{ room }` y solo muestra las entradas de la sala actual; Cabaña y Cocina tienen cada una su botón "?"; ícono de manito (mano_guante_transparente.png) marca qué entradas son clickeables
      <!-- js/scenes/HelpScene.js (ENTRIES/TITLES por room), CabinScene/KitchenScene con su propio _createHelpButton, RoomScene._makeHelpButton para salas futuras. Gotcha: Phaser dibuja las escenas por orden de registro en game.js, no por orden de scene.launch() — hubo que agregar scene.bringToTop('HelpScene') en cada punto de lanzamiento para que no quedara tapada por la sala. Dormitorio/Baño/Sala de actividades/Huerta quedan sin botón de ayuda hasta que tengan contenido real (decisión del usuario) -->
- [x] 2.15 Vero deja de aparecer en la Cabaña (pedido del usuario, revierte etapa-1 tasks.md 3.6/5.x): ahora vive dentro de las salas con contenido real. Cabaña se queda solo con fondo/carteles/stats/botones
      <!-- CabinScene: se sacó _createVero/_updateVeroExpression/_playBlink/playWalkAnimation y su llamada en create()/update(); _goToScene simplificado a fade directo sin animación de caminata (ya no hay Vero en Cabaña para animar). MapScene._goToRoom simplificado igual, sacando la dependencia de cabin.playWalkAnimation. KitchenScene ganó _placeVero/_updateVeroExpression/_playVeroBlink (mismo sistema de expresión reactiva a stats + parpadeo que tenía CabinScene, reubicado). Solo Cocina por ahora (mismo criterio que 2.14); las demás salas suman a Vero cuando tengan contenido real -->
- [x] 2.16 Transición entre Cabaña y salas simplificada a fade directo (~200ms), sin el paso intermedio de animación de caminata que dependía de Vero en la Cabaña (ver 2.15)
- [x] 2.17 Ajustes de pulido sobre 2.15 (pedido del usuario): tamaño/posición de Vero en Cocina, panel de comer rápido, y stats/config que faltaban en la sala
      <!-- (a) BUG: cada textura de Vero (idle/blink/expresiones) tiene tamaño nativo distinto (vero_idle 135x315 vs vero_idle1..4 67x130) — setTexture() sin reescalar hacía que Vero "se encogiera" un instante en cada parpadeo/cambio de expresión. Fix: _setVeroFrame(tex) recalcula el scale contra this._veroTargetH en cada cambio de textura, en vez de escalar una sola vez en _placeVero. Verificado con Playwright: displayHeight se mantiene en 260 en los 4 frames del parpadeo. (b) Panel de comer rápido: estado vacío ahora dice solo "Vacío" (antes "Nada para comer" + explicación larga); panel bajado ~12px (H-128 en vez de H-140) y más sólido (alpha 0.97 en vez de 0.9). (c) Stats y engranaje de configuración faltaban por completo en KitchenScene (solo existían en CabinScene, que ya no es donde el jugador pasa el tiempo). Se agregó _createGearButton (mismo ícono/comportamiento que Cabaña, esquina superior derecha) y _createStatsUI en columna vertical debajo de la monedita de ayuda (en vez de la fila horizontal de abajo, que ya está ocupada por el panel de comer). Como consecuencia, this.stats.update(delta) ahora también corre en KitchenScene.update() — si no, el decay de hambre/sueño/diversión quedaba congelado mientras el jugador está en la Cocina, que ahora es donde pasa la mayor parte del tiempo -->
- [x] 2.18 Fix 2 sobre el parpadeo de Vero: el fix de 2.17(a) eliminó el achicamiento pero introdujo un "zoom" — causa real es de los ASSETS, no de código: vero_idle.png deja aire debajo de los zapatos, vero_idle1..4.png (frames del parpadeo) vienen recortados mucho más ajustados, confirmado renderizando los 5 PNG lado a lado a igual altura. Se normalizó por ancho de cabeza (medido offline con Jimp, tabla VERO_HEAD_WIDTH) en vez de por altura de lienzo, solo para los frames del parpadeo — las expresiones de ánimo no se tocaron (no reportadas como problema)
      <!-- js/scenes/KitchenScene.js: VERO_HEAD_WIDTH + _placeVero (this._veroHeadTargetPx) + _setVeroFrame (usa headW si la textura está en la tabla, si no cae al escalado viejo por altura). Aproximado, no pixel-perfect — son recortes distintos, no la misma imagen a otra resolución. Si se agregan más frames de parpadeo/expresión y se nota el mismo salto, medir su ancho de cabeza igual y sumarlo a la tabla -->
- [x] 2.19 Fix 3 (final): el usuario reportó que 2.18 seguía sin resolverlo del todo — igualar el ancho de cabeza dejaba el ancho de HOMBROS/cuerpo ~13% más grande en los frames del parpadeo (vero_idle1-4 tienen una relación ancho/alto más "ancha" que vero_idle, no es solo la cabeza). Se probó además normalizar por ancho de cabeza las 6 expresiones de ánimo (feliz/cansada/aburrida/débil/colorada/sorprendida) y quedó peor todavía — "aburrida" con cabeza gigante, "colorada" enana — porque esas SÍ son dibujos con proporciones cabeza/cuerpo genuinamente distintas, no el mismo personaje recortado distinto. Conclusión: ningún escalado de una sola textura por otra puede resolver esto sin re-exportar assets. Se sacó el parpadeo por completo (decisión del usuario) — Vero queda fija en vero_idle, sin ciclar idle1-4. Verificado con Playwright: sin _blinkTimer, tamaño/textura de Vero no cambian con el tiempo
      <!-- js/scenes/KitchenScene.js: se eliminó VERO_HEAD_WIDTH, this._veroHeadTargetPx, el _blinkTimer en create(), y el método _playVeroBlink completo. _setVeroFrame volvió a ser un escalado simple por altura de lienzo (el único swap que queda es idle↔expresión de ánimo, que ya existía desde antes y no fue reportado como problema). Para reactivar el parpadeo en el futuro hace falta pedir vero_idle1-4 re-exportados con el mismo recorte/encuadre que vero_idle.png -->
- [x] 2.20 Feedback de testing sobre Cocina/Recetario (pedido del usuario, 5 cambios):
      <!--
      (a) Cocinar ya NO sirve el plato solo: Inventory.craft() ahora suma 1 del resultado al stock (antes solo lo devolvía y KitchenScene._cocinar le sumaba hambre directo a Vero). Los 5 productos 'plato' en products.js pasaron a comestibleDirecto:true con su propio campo `hambre` (copiado 1:1 de recetas.js) — aparecen en el panel de comer rápido una vez cocinados, y KitchenScene._eatQuick usa `product.hambre ?? 8` en vez del HUNGER_GAIN fijo, así que un plato cocinado da lo que valía la receta (verificado con Playwright: Ensalada da +18, no +8).
      (b) Frutas/verduras (categoría 'cosecha') no aparecían en ningún panel aunque sí tenían stock usable. Inventory.listByCategories() nueva (junta varias categorías) + _openStoragePanel ahora abre Heladera con ['heladera','cosecha'] en vez de solo 'heladera'. Efecto secundario encontrado y arreglado: con ~14 filas la lista desbordaba el panel modal (antes solo tenía 5) — _openModal ahora acepta un minContentH opcional y el panel crece hasta H-80 si hace falta.
      (c) Barra de timing de cocinar: targetWidth 0.22→0.14 (zona verde más chica) y speed 0.9→1.3 (cursor más rápido) — pedido explícito, la hace más difícil.
      (d) TimingBar ganó `useHitZone:false` (ya no se toca la barra) + método público `tap()`, y KitchenScene._cocinar agrega su propio botón "Cocinar" que llama a bar.tap(). TimingBar también ganó `cursorTexture` opcional para reemplazar el cursor rectangular blanco por una imagen — activado con `assets/sprites/gorro_sin_fondo.png` (textura 'gorro_cocinero', precargada en BootScene con filtro LINEAR).
      (e) Verificado extremo a extremo con Playwright: sembrar ingredientes → cocinar (bar+botón, camino de éxito Y de fallo) → plato aparece en comer rápido con stock 1 → comerlo da +18 (no +8) y descuenta el stock a 0.
      -->
- [x] 2.22 Bugfix: los paneles modales de Heladera/Alacena/Recetario (_openModal) dejaban pasar el click a lo que hubiera detrás (pedido del usuario, mismo patrón que 1.6 pero DENTRO de la escena)
      <!-- Bug: con la Heladera abierta, tocar en la posición de la Alacena/Mercadería la abría "por debajo"; tocar el engranaje/ayuda/pergamino (siempre visibles, fixed en pantalla) también funcionaba con cualquier modal abierto. A diferencia de 1.6 (HelpScene/SettingsScene/MapScene, escenas separadas → se arregló pausando la escena de atrás), acá heladera/hornito/mercadería y engranaje/ayuda/pergamino son objetos DENTRO de la MISMA escena (KitchenScene) — pausar no aplica. Fix: overlay de _openModal (this.add.rectangle...) pasa a .setInteractive() sin handler. Con topOnly (comportamiento default del InputPlugin de Phaser), solo el objeto de mayor depth bajo el puntero recibe el evento — el overlay (DEPTH.MODAL_OVERLAY=30, el más alto de la sala) lo intercepta y ningún objeto de atrás se entera. Sin tap-outside-to-close a propósito (mismo criterio ya documentado en 2.8 — MapScene tampoco lo tiene), solo la "✕" cierra. Verificado con Playwright: con Heladera abierta, click en la posición de Mercadería no cambia el título del modal; click en engranaje/pergamino no abre Settings/Map; el modal se mantiene igual hasta tocar la "✕" real. -->
- [x] 2.23 Bugfix: tocar las perillas del horno (lado derecho) abría la Alacena en vez del Recetario (pedido del usuario tras testing)
      <!-- Causa: KITCHEN_BOXES.hornito y KITCHEN_BOXES.mercaderia se solapan en una franja real (x: 0.0675-0.2131, y: 0.4309-0.5428 en fracciones del fondo — calculado con las cajas ya existentes, no una medición nueva), justo donde caen las perillas del lado derecho del horno. Las tres hover-zones (heladera/hornito/mercaderia) compartían el mismo depth (DEPTH.INTERACTIVE); con depth empatado, Phaser le da el hit test al objeto agregado más tarde en la lista — mercaderia se crea después que hornito en _createHoverZones, así que ganaba ella en toda la franja de solape. Fix: _makeHoverObject ahora acepta un `depth` opcional; hornito se crea con DEPTH.INTERACTIVE + 1 para que gane cualquier empate contra heladera/mercaderia. Verificado con Playwright: click en el centro exacto de la franja de solape abre Recetario, no Alacena. -->
- [x] 2.21 Bugfix panel de comer rápido (2 cambios, pedido del usuario tras testing):
      <!--
      (a) Descentrado: el ícono+nombre+cantidad tenía x fijo (panelX+52/+80), pegado a la flecha ◀ y lejos de la ▶/botón "Comer" — se veía descentrado. _createComerPanel ahora guarda _comerContentLeft/_comerContentRight (bordes reales de las flechas, medidos con .width) y el nuevo método _layoutComerContent(iconVisible) centra el bloque ícono+texto en ese espacio cada vez que cambia el producto (el ancho del bloque varía según el nombre, por eso se recalcula en cada _updateComerPanel en vez de dejarlo fijo). Verificado con Playwright en varios productos (Galletitas, Yogur, estado "Vacío") — capturas antes/después.
      (b) Verduras en el panel: tomate/lechuga/zanahoria/remolacha/choclo tenían comestibleDirecto:true por error (products.js) y aparecían en el ciclado de comer rápido junto a frutas/snacks/lácteos/platos. Pedido del usuario: solo deben aparecer comida rápida, fruta, snack, yogur y comida preparada — son ingredientes de cocina, no snacks. Cambiadas a comestibleDirecto:false (las frutas de cosecha — manzana, frutilla, mandarina, naranja, sandía — quedan true). Verificado con Playwright: Products.comestiblesDirecto() bajó de 19 a 14 ítems, sin ninguna verdura. NOTA para el usuario: Queso y Pan (categoría heladera) siguen comestibleDirecto:true — no los tocamos porque no estaban en la lista de verduras reportada y no es obvio si también deberían sacarse; confirmar.
      (c) NO reproducido en su momento (ver (d) más abajo, resultó ser la misma causa que (b)): el usuario reportó que seguía apareciendo la palabra "Vacío" (o similar) "de fondo" incluso cuando sí hay comida disponible. Se probó con Playwright el ciclo completo del catálogo con stock variado y la transición vacío→con stock recién agregado, y "Vacío" solo se mostraba cuando correspondía (stock total 0) — el bug real no era "Vacío" apareciendo de más, sino que el panel ciclaba TODO el catálogo (14 ítems) mostrando "Tenés: 0" para casi todos apenas UN producto tenía stock, en vez de mostrar solo lo disponible; eso es lo que el usuario percibía como ruido/confusión.
      (d) Rediseño pedido por el usuario tras cocinar y probar: (i) el panel debía mostrar SOLO productos con stock > 0 al ciclar (antes ciclaba los 14 del catálogo completo con "Tenés: 0" para casi todos); (ii) layout cambiado de ícono-izquierda + nombre/cantidad-derecha (se veía apretado y cortado con nombres largos) a nombre arriba / plato en el medio / cantidad abajo, todo centrado. Nuevo método `_availableComer()` (KitchenScene.js) filtra `Products.comestiblesDirecto()` por `inventory.get(id) > 0`; `_cycleComer`/`_updateComerPanel`/`_eatQuick` lo usan en vez del catálogo completo. Se eliminó `_layoutComerContent` (ya no hace falta recalcular X dinámicamente: con todo centrado en una columna, name/ícono/qty comparten el mismo `contentCenterX` fijo calculado una sola vez en `_createComerPanel`). Panel más alto (84→112) y bajado un poco (H-128→H-122) — ya no compite con el botón "Volver" de abajo porque ese botón ahora es el pergamino de arriba (ver 1.7). Verificado con Playwright: con 1 solo producto con stock, ciclar con las flechas se queda SIEMPRE en ese producto (no aparecen fantasmas con cantidad 0); agregar un segundo producto lo suma al ciclo; capturas confirman el layout vertical sin recortes. -->
      

## 3. Dormitorio

- [x] 3.1 Reemplazar el placeholder por el fondo real con objetos y decoración
      <!-- El usuario proveyó dormitorio.png (fondo completo, 1536x2752: cama, placard con puerta abierta, espejo, escritorio con vela/pluma, estantería con libros, ventana con luz cálida, cuadrito del hipopótamo con tutú, pila de ropa sucia) y muebles_sin_fondo.png (hoja con cama/placard/espejo/ropa sucia en transparente, pensada para superponer como en Cocina/Mapa).

      Comparé cada mueble de muebles_sin_fondo.png contra lo dibujado en el fondo (recorté regiones equivalentes de ambas imágenes y las puse una al lado de la otra) y NINGUNO calzaba: el espejo del fondo tiene marco simple ondulado reflejando la ventana, el de la hoja tiene marco ornamentado con corona y vidrio con brillos; la cama del fondo tiene un tallado de hoja en el respaldo que la de la hoja no tiene; la ropa sucia del fondo es una pila desparramada de prendas sueltas, la de la hoja es un bulto/atado redondeado. Son dibujos generados aparte, no recortes de la misma fuente — superponerlos habría creado un "mueble fantasma" duplicado (mismo problema, causa distinta, que 1.7 con Sala Actividades). Se descartó muebles_sin_fondo.png para los objetos interactivos.

      En su lugar, mismo criterio que KITCHEN_BOXES: cama/placard/espejo se recortan directo del fondo (assets/sprites/dormitorio_{cama,placard,espejo}.png vía Jimp) — alineación exacta por construcción, no por medición.

      Bug de contenido encontrado en el fondo: la pila de ropa sucia tenía el logo "sparkle" de Gemini dibujado DENTRO del arte (no un watermark de esquina fácil de recortar — estaba metido en un pliegue de una prenda gris, en x:1250-1420,y:2450-2600 aprox. de la imagen original). El usuario pidió que no se vea. Se generó assets/sprites/dormitorio_limpio.png (Jimp: se compuso un parche de 170x150 tomado de la misma pila, un poco a la izquierda, sobre la zona de la marca). El parche se nota si se hace zoom digital fuerte, pero a la escala real del juego (fondo escalado a H/tex.height ≈ 0.307 para entrar en pantalla) es imperceptible — verificado renderizando dormitorio_limpio.png con la misma fórmula de escala que usa RoomScene._drawBackground vía un canvas HTML aparte y comparando la captura. dormitorio_limpio.png (no dormitorio.png) es la textura 'dormitorio_fondo' que carga BootScene. -->
- [x] 3.2 Objetos interactivos: cama, placard, espejo
      <!-- BedroomScene reescrita para extender RoomScene (primera sala en aplicar la migración que RoomScene.js dejaba planteada desde 1.1). BEDROOM_BOXES (cajas relativas sobre dormitorio_limpio.png, 1536x2752) + _makeHoverObject para cama/placard/espejo, cursor de mano, sin efecto de hover-zoom (mismo criterio que Cocina). Callback de cada uno por ahora es un stub (_stub(msg): texto flotante tipo "💤 Dormir (próximamente)") — las mecánicas reales (dormir, customización) son 3.5/3.6/3.7, todavía no diseñadas en detalle con el usuario. También: _makeHelpButton('bedroom') + _makeBackButton (pergamino, heredados de RoomScene) y nuevas entradas HelpScene.ENTRIES.bedroom/TITLES.bedroom. Verificado con Playwright: fondo y los 3 objetos se ven, cada uno muestra su mensaje al tocarlo, Ayuda contextual muestra "Ayuda — Dormitorio" y pausa la sala igual que en Cocina.

      PENDIENTE (bultito de ropa sucia como objeto interactivo aparte): dado el problema de 3.1, no se agregó como cuarto hover-object — queda solo como parte visual del fondo (ver 3.4, que de todos modos dice que no tiene efecto en stats, así que no hace falta que sea clickeable).

      Fix post-entrega: el usuario pidió que Dormitorio tenga stats + engranaje igual que Cocina (KitchenScene._createGearButton/_createStatsUI/_updateStatsUI/_checkGlucemiaShake existían solo ahí, copy-paste inminente para cada sala nueva). Se subieron a RoomScene como _makeGearButton/_createStatsUI/_glucemiaColor/_updateStatsUI/_checkGlucemiaShake (mismo criterio que _makeHelpButton/_makeBackButton) para que Baño/Sala Actividades/Huerta los hereden gratis en vez de reimplementarlos cada vez — KitchenScene NO se tocó (sigue con su copia propia, no vale la pena el riesgo de refactor sobre código que ya funciona a esta altura). BedroomScene.create() ahora setea this.stats = this.registry.get('stats'), llama _makeGearButton(W) + _createStatsUI(), y agregó update(time, delta) igual que KitchenScene (this.stats.update(delta) + _updateStatsUI() + _checkGlucemiaShake(), si no el decay de hambre/sueño/diversión se congela mientras el jugador está en el Dormitorio). Verificado con Playwright: engranaje abre Configuración y pausa la sala, columna de stats visible debajo de la monedita de ayuda.

      Fix post-entrega (2): el usuario pidió agregar a Vero en la escena. KitchenScene._placeVero/_setVeroFrame/_updateVeroExpression (sin parpadeo, decisión ya tomada — ver 2.19) también se subieron a RoomScene, generalizando _placeVero para recibir la posición de pie (x,y) en vez de calcularla contra el panel de comer rápido (específico de Cocina). BedroomScene llama this._placeVero(W/2 - 20, H - 40) en create() (parada sobre la alfombra frente a la cama/escritorio, corrida un poco a la izquierda para no superponerse tanto con el placard) y this._updateVeroExpression() en update(). Verificado con Playwright: Vero aparece de pie con la textura idle; forzar stats.sleep=10 (isSleepy()) cambia la textura a vero_cansada correctamente, igual que en Cocina. -->
- [x] 3.3 Decoración fija: escritorio, estantería de libros, ventana con luz dinámica, cuadrito hipopótamo con tutú
      <!-- Escritorio/estantería/cuadrito ya estaban dibujados en dormitorio_limpio.png. Ventana dinámica: RoomScene._makeWindowLight/_updateWindowLight (generalizado desde KitchenScene._updateSky, que no se tocó). El fondo original NO tenía recorte transparente en el vidrio (a diferencia de cocina_nueva.png) — se generó assets/sprites/dormitorio_ventana.png vía Jimp, recortando a alpha=0 los píxeles color-vidrio (r>190,g>140,b>60) dentro de la caja del vidrio, restringido a x>=1385/1536 para no comerse el visillo (sus resaltos son casi tan claros como el vidrio; un primer intento con la caja completa perforó la cortina). dormitorio_fondo ahora carga ese archivo en vez de dormitorio_limpio.png. Verificado con Playwright: fillColor cambia según la hora real (bfe3ff día/ff9944 tarde/151538 noche), sin agujeros en la cortina.

      Hallazgo importante (no un bug de este fix): el fondo (1536x2752) es más ancho en proporción que el viewport del juego (390x844) — RoomScene._drawBackground centra y recorta ~40px de cada lado. La ventana está pegada al borde derecho del arte (x 1388-1489 de 1536), así que en el juego solo se ve una tira de ~5px de la luz dinámica; el resto queda fuera de cámara, igual que ya le pasa al lado derecho del placard. No se tocó nada para "arreglarlo" porque no es un bug de este task, pero vale saberlo para el próximo fondo con ventana (Baño) — conviene pedirle al usuario que el elemento importante no quede tan pegado al borde derecho/izquierdo del arte. -->
- [x] 3.4 Ropa sucia: solo visual/decorativa (bultito presente hasta que se lava/guarda), sin efecto en stats
      <!-- Decisión del usuario (1.4): no baja ninguna stat, es ambientación. Visible en el fondo (dormitorio_limpio.png), sin hover-object propio — ver nota de 3.2 -->
- [x] 3.5 Dormir sola: Vero se acuesta + Ámbar sube a los pies de la cama → sube sueño
      <!-- BedroomScene._dormirSola. Reusa _runSleepFade (extraído de _sleepTogether, ver 3.6) con fondo 'dormitorio_sola' (habitacion-durmiendo-sola-limpia.png, asset del usuario — mismo cuarto/estilo, Vero sola + Ámbar enroscada a los pies de la cama YA dibujada en el fondo, no hace falta sprite de Ámbar aparte). Solo +20 sueño (spec: sin diversión, a diferencia de dormir con Seba). La cama ahora abre un panel de elección chico (_openSleepChoice, 2 botones: "💤 Dormir sola" / "🤗 Dormir con Seba") en vez de ir directo a una sola mecánica — reemplaza el wireo temporal de 3.6. Verificado con Playwright: menú se abre bien, dormir sola sube sueño a 100 sin tocar diversión, dormir con Seba sigue andando igual desde el menú (sin regresión), sin errores de consola. -->
- [x] 3.6 Dormir con Seba: secuencia dron + caja (estampilla + 🇦🇷) → caja se rompe → aparece Seba → abrazo → sube sueño + diversión
      <!-- BedroomScene._dormirConSeba/_dropAndExplode/_appearSeba/_hugSeba. Assets provistos por el usuario (drone_sin_fondo.png, caja_abierta_sin_fondo_v2.png), cuadrados 2048x2048. NO se usa caja_sin_fondo.png (caja sola cayendo): su ángulo de frente no calzaba con el isométrico de la caja en drone_sin_fondo.png — se pidió una v2 en el mismo ángulo y no salió bien, así que en vez de reintentar el arte se resolvió en código: la secuencia salta directo de "dron con caja" a "explosión" (cameras.main.flash+shake tapando el corte de ángulo), sin frame intermedio.

      Bug de assets encontrado: seba.png (textura 'seba', ya precargada desde antes pero nunca renderizada en ninguna escena) resultó ser la hoja de referencia completa del personaje (título, recuadro con fondo crema, 4 miniaturas de expresión) — NO un sprite recortado. Se generó assets/sprites/seba_cuerpo.png vía Jimp: recorte del panel "SPRITE DE CUERPO COMPLETO: LLEGADA Y ABRAZO" + fondo crema pasado a transparente por umbral de distancia de color (con banda de suavizado en el borde para no dejar un recorte serruchado). BootScene ahora carga ese archivo bajo la misma clave 'seba'.

      Seba aparece con pop-in (scale+alpha) al lado de Vero, se acerca con un tween, pulso de escala breve en los dos ("apretón"), +20 sueño +20 diversión con feedback flotante. Verificado con Playwright (3 corridas limpias, stats llegan a 100/100 como se espera desde el default de StatsSystem).

      Fix post-entrega: el usuario no quedó conforme con seba_cuerpo.png (extraído de la hoja de referencia original) y mandó sprites-seba.png, una hoja nueva con 8 poses en grilla (frente/3-4/espalda/perfil arriba, 4 frames de caminata abajo) en el mismo estilo chibi que Vero — mucho mejor matcheo visual. Se recortó solo la pose de frente (celda x:206-422,y:32-520 de la hoja 1536x1024, con margen) a assets/sprites/seba_frente.png — ya venía con transparencia real, no hizo falta chroma-key. BootScene ahora carga esa como 'seba'. El resto de las poses (3-4/espalda/perfil/caminata) quedan sin recortar hasta que haga falta alguna.

      Tramo final agregado (no estaba en el alcance original de 3.5/3.6 pero lo pidió el usuario para redondear la mecánica): tras el abrazo, un rectángulo negro (nunca llega a alpha 1.0 — pedido explícito: "que dé sentido a que se está durmiendo", no un corte a negro total) oscurece la pantalla (alpha 0.85), en lo oscuro Seba se destruye, Vero y los objetos interactivos viejos (cama/placard/espejo) se ocultan y el fondo se swapea a `dormitorio_pareja` (habitacion-pareja-durmiendo.png, asset nuevo del usuario — mismo cuarto/estilo, Vero y Seba acostados juntos). Se aclara a alpha 0.45 (dormidos, no de día), se mantiene 1.6s, oscurece de nuevo, y al aclarar del todo vuelve el fondo original + Vero visible sin Seba + se limpia _veroExpressionOverride. `_swapBackground()` nuevo en BedroomScene recalcula escala/offset para la textura nueva sobre el mismo Image (dormitorio_pareja es 938x1677, aspecto casi idéntico a dormitorio_ventana.png así que no desentona). Se agregó guarda `_sleepSequenceActive` para ignorar un segundo tap en la cama mientras la secuencia ya está corriendo (antes no existía, se podían disparar dos secuencias superpuestas). Verificado con Playwright: 3 corridas limpias de punta a punta, estado final correcto (stats 100/100, Vero visible, override limpio, guarda liberada).

      PENDIENTE: la cama dispara esta secuencia DIRECTO (bypaseando el menú "dormir sola" vs "dormir con Seba", que es 3.5 y todavía no arrancó) — hay que reemplazar ese wireo cuando 3.5 exista. -->
- [ ] 3.7 Armario como acceso a customización de ropa (stub; funcionalidad completa en Etapa 3)
      <!-- Stub temporal en 3.2 (_stub('👗 Personalizar ropa (próximamente)')) ya cumple el criterio de "stub" de esta tarea; falta la funcionalidad completa de Etapa 3 -->
- [x] 3.8 Botón volver a la cabaña
      <!-- Ahora es el pergamino → abre MapScene (RoomScene._makeBackButton, ver 1.7), no un botón de texto propio -->

## 4. Baño

- [x] 4.1 Reemplazar el placeholder por el fondo real con objetos y decoración
      <!-- El usuario proveyó baño.png (1536x2752): bañera de patas con canilla dorada, inodoro, espejito de madera sobre bacha, más decoración fija (plantitas, toallas, luz cálida de pared, alfombra) — todo ya dibujado en el fondo, sin hoja de objetos aparte (a diferencia de Cocina/Dormitorio no hizo falta descartar una segunda imagen). Tenía el logo "sparkle" de Gemini en la esquina inferior derecha, sobre el piso de madera — a diferencia de los logos en tela del Dormitorio (pliegues irregulares, costó mucho limpiarlos bien), este quedó perfecto: el piso tiene veta diagonal repetitiva, así que un parche rectangular recortado de la misma tabla (corrido 150px horizontal, sin ajustar el ángulo) alcanzó para que las líneas sigan derecho sin costura visible ni siquiera de cerca. bano-limpio.png reemplaza a baño.png. -->
- [x] 4.2 Objetos interactivos: bañera, inodoro, espejito; decoración fija: plantitas, luz cálida
      <!-- BathroomScene reescrita para extender RoomScene (mismo patrón que BedroomScene: BATHROOM_BOXES + _makeHoverObject, hereda _makeHelpButton/_makeGearButton/_createStatsUI/_placeVero de RoomScene gratis). bano_banera/inodoro/espejo recortados directo del fondo (Jimp), mismo criterio que BEDROOM_BOXES. Callbacks son stubs por ahora (_stub, mismo texto flotante que Dormitorio) — las mecánicas reales (bañarse en 3 pasos, inodoro, elegir peinado con swipe) son 4.3-4.9, todavía sin diseñar en detalle. Se agregaron las entradas de HelpScene.ENTRIES/TITLES.bathroom (de paso se actualizó el texto de la entrada de la cama del Dormitorio, que había quedado desactualizado — decía "próximamente" pero dormir ya es una mecánica real). Vero parada sobre la alfombra en (200,790). Verificado con Playwright: fondo y los 3 objetos se ven, cada uno muestra su stub al tocarlo, Ayuda contextual muestra "Ayuda — Baño" con las 3 entradas nuevas, sin errores de consola. -->
- [x] 4.3 Bañarse paso 1: cepillar el pelo (N barras de timing según peinado)
      <!-- BathroomScene._banarse/_brushStep. N fijo en 2 (no en el peinado): el spec pide "N según el peinado", pero el peinado variable depende de 4.9, diferido a propósito (sin sprites de peinados alternativos) — decisión del usuario. Resultado de cada barra (éxito/fallo) no bloquea el flujo, siempre pasa a la siguiente. -->
- [x] 4.4 Bañarse paso 2: frotar shampoo con gesto swipe
      <!-- BathroomScene._shampooStep, SwipeGesture con contador (5 swipes) + círculo animado de feedback. Bug real encontrado y arreglado en el camino: la zona de swipe (depth 0 por defecto en SwipeGesture) quedaba tapada por los hover-objects de bañera/inodoro (depth 5, siguen activos durante el minijuego) en la mitad de su área — Phaser (topOnly) le mandaba el pointerdown al objeto de mayor depth ahí, no a la zona nueva, y el swipe se perdía en silencio sin ningún error. Se le agregó un parámetro `depth` a SwipeGesture (ver js/systems/SwipeGesture.js) — BathroomScene la usa con depth:56. Verificado con Playwright a nivel de eventos crudos de Phaser (zone.on('pointerdown') vs scene.input.on('pointerdown')) para confirmar la causa antes de arreglarlo. -->
- [x] 4.5 Bañarse paso 3: resto del baño con barra de timing → sube higiene
      <!-- BathroomScene._finalBathStep/_finishBath. "Sube higiene" = +10 hambre/sueño/diversión (decisión ya tomada en design.md Open Questions: sin stat de higiene propia). Verificado con Playwright: secuencia completa (cepillar x2 + shampoo x5 + barra final) deja las 3 stats en 80→90, _bathActive vuelve a false. -->
- [x] 4.6 Inodoro: sentarse (animación, carita colorada 😳 si aplica)
      <!-- BathroomScene._usarInodoro: sin sprite de "sentada" (no existe), se resuelve con texto flotante + 50% de chance de mostrar 😳 ("si aplica" = al azar, gag sin trigger de stats). -->
- [x] 4.7 Inodoro: tirar la cadena (barra de timing)
      <!-- BathroomScene._flushStep. Decisión del usuario: el inodoro completo (sentarse/cadena/sopapa) NO sube ninguna stat, es pura interacción de humor. -->
- [x] 4.8 Inodoro: destaparse con sopapa (barra de timing) cuando se tapa
      <!-- BathroomScene._unclogStep, sin asset de sopapa (se resuelve con el emoji 🪠 en el feedback). Se tapa con más chance si se falló la barra de tirar la cadena (50% si falló, 15% si no) — le da consecuencia al resultado de 4.7 en vez de ser puro azar. -->
- [x] 4.9 Espejito: elegir peinado ciclando opciones con swipe
      <!-- Sin sprites de peinados alternativos (ver nota de 4.3) — el usuario eligió que el swipe cicle solo el NOMBRE del peinado (texto), sin cambiar el aspecto de Vero, en vez de dejarlo en stub. BathroomScene._elegirPeinado: picker con SwipeGesture (1.3) ciclando PEINADOS=['Suelto','Colita','Trenza','Rodete'], índice guardado en game.registry (sobrevive a cambiar de sala, sin persistir en SaveManager — sin efecto de gameplay todavía). Verificado con Playwright: swipe cicla las 4 opciones con wrap en ambas direcciones, cierra sin dejar overlay residual, reabrir muestra la última selección. -->
- [x] 4.10 Botón volver a la cabaña
      <!-- Ya presente en el placeholder -->

## 5. Sala de actividades

- [x] 5.1 Reemplazar el placeholder por el fondo real con decoración
      <!-- El usuario proveyó sala-actividades.png (1536x2752), generado a partir de un prompt armado en esta sesión (cozy cabin game room, todo en una sola imagen — mismo criterio que Dormitorio/Baño, sin hoja de objetos aparte). Tenía el logo "sparkle" de Gemini sobre la tierra de la maceta de abajo a la derecha; a diferencia de Dormitorio/Baño (parche clonado de tela/madera), acá no había veta repetitiva que copiar, así que se resolvió con inpaint por difusión iterativa (clean-sala-logo.js, Jimp: cada píxel del logo se promedia con sus vecinos hasta reconstruir el degradé de la tierra) — sala-actividades-limpia.png es la textura 'sala_fondo' que carga BootScene. ActivityScene reescrita para extender RoomScene (mismo patrón que BedroomScene/BathroomScene). -->
- [x] 5.2 Decoración fija no clickeable: estantería, caballete, compu/tablet, tela del techo, binoculares/cámara, food truck
      <!-- Todos ya dibujados en sala-actividades-limpia.png — mismo criterio que escritorio/estantería/cuadrito en Dormitorio (3.3): decoración puramente visual, sin GameObject ni hover-box propio (no son interactivos, no hace falta recortarlos aparte del fondo). -->
- [x] 5.3 Ícono de juego de mesa que abre el menú de minijuegos (stub "próximamente"; minijuegos en Etapa 4)
      <!-- Única pieza interactiva de la sala (design D7). ACTIVITY_BOXES.mesa recortada directo del fondo (extract-sala-mesa.js, mismo criterio que BATHROOM_BOXES/BEDROOM_BOXES) — textura 'sala_mesa'. _makeHoverObject dispara _stub('🎲 Minijuegos (próximamente)'). Ayuda contextual agregada (HelpScene.ENTRIES/TITLES.activity). Verificado con Playwright: fondo sin logo ni texturas rotas, Vero (90,780) sin superponerse con muebles, tap en la mesa muestra el stub y se desvanece, ayuda muestra "Ayuda — Sala de actividades" con la entrada de la mesa, pergamino/mapa/volver sin dejar estado colgado, sin errores de consola. -->
- [x] 5.4 Botón volver a la cabaña
      <!-- Ya presente en el placeholder -->

## 6. Huerta

- [x] 6.1 Reemplazar el placeholder por el fondo exterior real con canteros
      <!-- El usuario proveyó huerta.png (1536x2752, huerta exterior detrás de la cabaña, 6 canteros de madera en arreglo diamante), generado con un prompt armado en esta sesión (mismo estilo que las otras salas, canteros bien separados para poder recortarlos después). Tenía el logo "sparkle" de Gemini sobre follaje denso (esquina inferior derecha) — el caso más difícil de limpiar hasta ahora: el follaje es textura irregular con una ramita cruzando cerca, ni difusión ni clonado directo funcionaron bien (ver clean-huerta-logo.js, documenta los 5 intentos). Se resolvió con máscara por saturación (sparkle desaturado vs. follaje/ramita saturados) + relleno en bloques 4x4 sorteados del anillo alrededor de la propia máscara → huerta-limpia.png, textura 'huerta_fondo'. GardenScene reescrita para extender RoomScene (mismo patrón que las demás salas). -->
- [x] 6.2 Implementar `plant-growth-system`: tiempos por especie + madurez derivada del reloj real
      <!-- js/data/plants.js — PLANTS (10 especies, mismos ids que products.js categoría cosecha) con growthMs según la tabla del proposal (30 min lechuga a 12 hs sandía). Plants.isReady/progress derivan SIEMPRE contra Date.now() - plantedAt (design D5) — sin acumulador, una planta sigue madurando con el juego cerrado. -->
- [x] 6.3 Persistencia del estado de la huerta en localStorage (esquema versionado con timestamps)
      <!-- Resuelve la Open Question de design.md sobre la clave de storage: en vez de una clave separada 'juego-vero:garden' (sugerida en D5), se unificó en SaveManager.js (ya existía para stats+inventory) bajo la misma clave versionada 'juego-vero-save-v1' — evita duplicar los 3 disparadores de autoguardado de game.js. game.registry.get('garden') es un array plano de 6 (GardenScene.PLOT_COUNT), cada uno `{species, plantedAt, watered}` o null. GardenScene guarda inmediatamente tras cada plantar/regar/cosechar, además del autoguardado periódico. Verificado con Playwright: recarga completa de página + renavegación mantiene el estado exacto. -->
- [x] 6.4 Comprar semillas con monedas
      <!-- Semillas GRATIS por ahora — decisión conversada con el usuario: el sistema de monedas/mercado queda para después de Huerta (no tiene sentido gatear con una moneda que todavía no se puede ganar, ver Open Question 2 de design.md, sin resolver). El picker de semillas (_openSeedPicker) ya lista las 10 especies con ícono/nombre/tiempo; cuando exista el sistema de monedas, ese mismo picker suma el costo y descuenta inventory.coins antes de plantar — comentario en el código marca el punto de enganche. -->
- [x] 6.5 Plantar semilla en cantero libre (registra instante de plantado)
      <!-- GardenScene._plant: tap en un cantero vacío abre el picker de semillas; elegir una hace garden[i] = {species, plantedAt: Date.now(), watered:false} + guarda + feedback flotante. -->
- [x] 6.6 Regar plantas (estado visual)
      <!-- GardenScene._water: resuelve la Open Question de design.md sobre si el riego es obligatorio o acelera el crecimiento — NINGUNA de las dos, tasks.md ya decía "estado visual" explícito. Tap en un cantero creciendo pone watered:true (no toca Plants.isReady en absoluto) y muestra una gotita 💧 al lado del brote + feedback flotante. -->
- [x] 6.7 Cosechar planta lista → producto va al inventario de la cocina
      <!-- GardenScene._harvest: tap en un cantero listo (Plants.isReady) hace inventory.add(species,1) — mismo Inventory compartido con la Cocina — limpia el cantero y muestra "+1 Nombre 🎉". -->
- [x] 6.8 Aviso de planta lista (in-game en esta etapa; push nativa diferida a Etapa 5)
      <!-- Banner "🌱 N lista(s) para cosechar" arriba de la pantalla, visible mientras haya al menos un cantero listo — se actualiza cada 1s (this.time.addEvent, solo refresca la UI, no es un acumulador de crecimiento). D6 ya dejaba la notificación push real del SO para Etapa 5 (depende de Capacitor). -->
- [x] 6.9 Botón volver a la cabaña
      <!-- Ya presente en el placeholder -->

## 7. Criterio de éxito (verificación)

- [x] 7.1 Todas las salas se pueden visitar y tienen sus objetos (Cocina, Dormitorio, Baño, Sala de actividades y Huerta con contenido real)
- [ ] 7.2 Las mecánicas básicas de cada sala funcionan y afectan las stats
      <!-- Cierto para Cocina/Dormitorio/Baño (hambre/sueño/diversión) y parcialmente Huerta (cosecha alimenta el inventario de Cocina, efecto indirecto en hambre). Sala de actividades es la excepción a propósito: su única mecánica es el acceso a minijuegos (design D7), que son Etapa 4 — hoy no toca ninguna stat porque no hay mecánica real todavía, no es un bug. -->
- [x] 7.3 Ámbar aparece en la cocina
- [x] 7.4 Ámbar/Seba aparecen en la escena de dormir
      <!-- Ya cubierto por 3.5 (Ámbar enroscada a los pies en dormitorio_sola) y 3.6 (Seba en la secuencia dormir con Seba) -->
- [x] 7.5 La huerta planta, crece y cosecha correctamente
      <!-- Verificado con Playwright: plantar, regar (visual), fast-forward de plantedAt para simular maduración, cosechar (suma al inventario compartido con Cocina), dos canteros independientes, y persistencia real tras recargar la página. -->
